---
title: 世界模型是什么：接口、渲染、模拟与规划
description: 先用状态估计、转移、发射与预测查询建立工作定义，再区分渲染器、模拟器和规划器三种功能。
lecture: 1
---

# 世界模型是什么：接口、渲染、模拟与规划

一个模型生成的视频看起来很真实，它就是世界模型吗？一个机器人策略能直接输出动作，却从不预测动作的后果，它有没有世界模型？一个物理引擎从未用数据训练，但可以准确模拟碰撞，又该放在哪里？

这些问题无法靠模型名称回答。我们需要同时检查它表示什么、预测什么、是否接受动作条件，以及预测结果怎样进入决策。World Labs 提出的渲染器、模拟器、规划器三分法提供了一个功能视角，本页再为后续课程补上一套可以落实到代码的接口视角。

## 全课程共用的工作契约

强化学习里的 POMDP（部分可观测马尔可夫决策过程）提供了基本循环：智能体采取动作，动作改变隐藏的世界状态，状态产生观察，观察再影响下一次行动。**状态**是决定未来演化的环境变量，**观察**是传感器提供给智能体的不完整证据。两者的差异会在 L02 进一步展开。

本课程把世界模型视为一组可以检查和替换的接口：

| 接口 | 输入与输出 | 回答的问题 |
| --- | --- | --- |
| 状态估计或编码 | 观察与历史 → 潜在状态 | 世界现在可能处于什么状态？ |
| 转移模型 | 潜在状态与动作 → 下一状态分布 | 如果采取这个动作，会发生什么？ |
| 发射或解码 | 潜在状态 → 可观察量 | 这个状态看起来是什么样？ |
| 预测查询 | 历史与候选动作 → 未来轨迹或任务信号 | 哪些未来与当前决策有关？ |

并非每个系统都显式实现四个接口。JEPA 可以不重建像素，MuZero 只预测规划需要的量，物理模拟器的转移由方程而不是神经网络给出。这个契约的用途不是排除异类，而是让比较具体起来：系统省略了什么、替换了什么，以及下游如何使用它的预测？

## 常见误区

围绕上面这份契约，有三种误读反复出现，值得专门点出来。

**"世界模型就是视频预测器。"** 视频预测只是发射接口的一种可能实现，不是整个系统。一个只预测像素、从不以动作为条件、也不支持"如果这样做会怎样"这类转移查询的模型，做出来的是渲染器，而不是本课程关心的转移和查询接口。根源在于把最显眼的输出，也就是视频，当成了定义本身，而真正的定义关心的是存在哪些接口、这些接口支持什么预测。

**"如果系统不重建观察，它就没有世界模型。"** 上面接口表的第二列已经回答了这个问题：JEPA 可以从不解码回像素，却仍然拥有状态估计器和转移模型；MuZero 只预测规划所需的价值和奖励信号，从不输出图像。重建只是众多可能的发射接口之一，不是必要条件。

**"一个不显式做预测的策略，不可能包含世界模型。"** 这混淆了架构和功能。无论转移模型是一个专门命名的模块，还是端到端策略里的一层隐藏表示，或者只是损失函数里隐含的一项，真正要问的是：系统是否预测了后果，并让动作以这个预测为条件。LeCun 对 VLA 架构的批评正是针对这一点：参数量很大，却没有内部转移模型可以推理真正陌生的情况，只能退回到记忆匹配。

这三种误区共享同一个根源：把一个可见的实现细节，像素进、像素出、某个具体命名的模块，误当成了功能契约本身。本页接下来通过三种具体的功能类型来追踪这份契约。

## 三种功能性的世界模型

计算机视觉、机器人学、强化学习、生成 AI，这几个领域都声称在开发"世界模型"，但各自指代的东西截然不同。混乱的根源在于对"世界"本身定义的模糊。

当今被称为"世界模型"的各类系统，本质上都是这个循环的不同输出。基于此，李飞飞的文章区分出三种功能性类型。

### 渲染器：输出人能看懂的观察

渲染器的职责是输出观察信息，通常以像素形式呈现。衡量渲染器好坏的主要标准是视觉保真度。

文生视频模型（Veo、Sora）是渲染器，交互式生成系统（Genie、World Labs 的 RTFM）是渲染器。它们的共同特点是对三维结构没有显式理解，呈现的是"看起来像什么"，而不是"实际是什么"。这就是为什么 AI 生成的城市在俯视图上完美无缺，但从内部驾驶却会发现建筑崩坏、街道几何违反物理。

### 模拟器：输出符合物理规律的状态

模拟器输出的是世界状态本身，在几何、物理或动力学上忠实于现实。相比渲染器只需视觉说服力，模拟器要满足更严格的结构约束：几何关系必须经得起推敲，物理过程必须遵守牛顿定律，动态行为必须符合因果规律。

模拟器的用户是两类：建筑师、工程师、游戏开发者，他们需要超越视觉真实感的准确性。以及强化学习智能体、机器人控制器、自动驾驶系统，它们需要在虚拟环境中安全测试真实场景中危险或昂贵的情况。

Dreamer 系列（V1–V4）训练策略的"梦境"正是一种隐式模拟器：它在潜在空间里维护状态表示，根据动作推演下一个状态，策略在这个内部仿真里学习，然后迁移到真实环境。

### 规划器：输出智能体应该采取的动作

规划器输出的是动作，给定当前观察和目标，智能体下一步应该怎么做。某种意义上，规划器是渲染器的逆过程：渲染器把动作转换为观察。规划器把观察转换为动作，由此闭合感知-行动循环。

VLA（Vision-Language-Action，以视觉观测和语言指令为输入、直接输出机器人动作的端到端模型）是规划器，CEM-MPC 和 TD-MPC（两种基于世界模型的规划算法，L03 涵盖其核心机制）是规划器，Dreamer 里的潜在 Actor-Critic 也是规划器。规划器是三类中最难做好的。目前看起来令人印象深刻的机器人演示，几乎都局限于高度受控的实验室环境。演示视频与能在真实厨房、仓库、手术室可靠工作的机器人之间，仍有巨大的鸿沟。


## 模拟器是关键

三类可以单独界定，但它们共享同一个根基：对世界如何运作的深层认识，几何、物理、动力学。一个真正理解世界的模型应该能够完成所有三项任务：从多个角度渲染一个杯子的样子，模拟杯子被推动时会发生什么，以及规划一只手该如何拿起这个杯子。

在三者中，模拟器获得的商业关注最少，但在功能上最为关键。原因是方向性的。

渲染器优化视觉可信度，不要求物理准确性。这个天花板是真实的：渲染器的输出足够美观，却不足以用于机器人训练或工程设计。

规划器最具吸引力，但没有一个对世界如何运作的内部模型，规划器只能靠记忆情境和模式匹配来输出动作。这正是 LeCun 批评 VLA 路线的核心论点：它记住了海量的驾驶场景，却没有内部的因果模型，一旦遇到真正陌生的情况，它没有推理后果的能力。

模拟器是连接两者的桥梁。如果语言是对世界的抽象，像素是对世界的投影，那么几何、物理和动力学就是世界本身。模拟器在这个层面上运作，提供那个结构骨架，从中既可以衍生出视觉呈现供人类消费，也可以衍生出动作后果供智能体使用。


## 当前领域的最前沿：边界在融合

李飞飞的文章里有一个值得注意的观察：当前最有趣的研究正在有意模糊这三个类别的边界。

World Labs 的 Marble（一个从单张或少量图像重建三维场景的生成模型）已经从单个模型同时输出高斯散射（用于渲染）和碰撞网格（用于物理模拟），一个输出服务视觉，另一个服务物理引擎。一些机器人实验室的最新工作表明，预训练的视频渲染器可以直接作为动作预测的骨干，把渲染器和规划器的职责合并进同一个模型。

两条线指向同一个方向：一个模型，能渲染、能模拟、能规划，根据下游需要切换输出。


## 一个哲学脚注

李飞飞的三分法并非偶然，它和认识论里的一个经典三角有对应关系。

**模拟器回答的是本体论问题：世界本身是什么。** 独立于观察者存在的客观物理结构，有空间、力学和因果的固有规则。

**渲染器回答的是现象论问题：世界看起来是什么。** 人通过感官得到的表象，视觉显像，我们肉眼看见的一切都是世界在感知维度上的投影。

**规划器回答的是实践论问题：主体能做什么。** 立足表象、面向客观世界的行动能力，主体通过实践作用于世界、改造世界。

Craik 在 1943 年用"外部现实的小规模模型"描述人脑的预测机制时，指向的正是这三个维度的统一体：在本体层推演，在现象层呈现，在实践层输出行动建议。谢赛宁所说的"终点"，在这个意义上早已有了答案，只是工程手段用了八十年才追上这个直觉。


## 延伸阅读

- Li, F.-F. et al., World Labs (2025). [What Is a World Model?](https://x.com/drfeifei/status/2062247238143996275)：三种功能性世界模型的系统定义
- 谢赛宁访谈（2024）. [世界模型、具身智能与 AMI Labs](https://www.youtube.com/watch?v=rIwgZWzUKm8)：谢赛宁对"世界模型是所有人都会抵达的终点"这一判断的完整阐述
- Ha & Schmidhuber (2018): World Models（见 L01 延伸阅读）：最早把渲染（V）、模拟（M）、规划（C）清晰分离的工程框架
